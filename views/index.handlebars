<h1></h1>

<h2>Choose a Burger to eat!</h2>

<ul>
  {{#each burger}}
    {{#unless eaten}}
      {{> burger-block eaten=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been DEVOURED!</h2>

<ul>
  {{#each burger}}
    {{#if eaten}}
      {{> burger-block sleep=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="bu">Burger Type:</label>
    <input type="text" id="burger" name="name">
  </div>

  <div class="form-group">
    <label for="sleepy">Has this burger been eaten?</label><br>
    <input type="radio" name="eaten" value="1" checked> yes!<br>
    <input type="radio" name="eaten" value="0"> no
  </div>

  <button type="submit">Add Burger</button>
</form>