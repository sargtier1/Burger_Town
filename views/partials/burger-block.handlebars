<li>
	{{name}}

	<buttonv id="change-eaten" class="change-status" data-id="{{id}}" data-eaten="{{eaten}}">
		{{#if eaten}}Burger has been eaten!{{else}}You haven't eaten a burger!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>